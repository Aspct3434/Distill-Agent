# Test / lint / typecheck toolchain. Installed on top of requirements.txt by
# CI and local dev environments; never needed at runtime.
hypothesis==6.155.2
mypy==2.1.0
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.15.16
